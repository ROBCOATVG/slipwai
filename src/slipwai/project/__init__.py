"""The parts of the repository a generated project is made of.

One module per part — its Makefile, its Compose file, its docs, its commands, its backing-service
adapters, its backend language — because that is what changes together. `slipwai.scaffold`
assembles them into the file map that gets written.
"""
