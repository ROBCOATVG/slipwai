# Ward board

A desktop application for a ward's admissions board, built with Apache Ant from a NetBeans project layout:
`ant compile` compiles it, `ant jar` packages it, and the jars under `lib/` are its classpath.
